# Systemd service for airband real-time sync
#
# Installation:
#   1. Copy this file: sudo cp airband-sync.service.example /etc/systemd/system/airband-sync.service
#   2. Edit paths and user: sudo nano /etc/systemd/system/airband-sync.service
#   3. Create env file: cp airband-sync.env.example ~/.config/airband-sync.env
#   4. Edit env file: nano ~/.config/airband-sync.env
#   5. Reload systemd: sudo systemctl daemon-reload
#   6. Enable on boot: sudo systemctl enable airband-sync
#   7. Start service: sudo systemctl start airband-sync
#   8. Check status: sudo systemctl status airband-sync
#   9. View logs: sudo journalctl -u airband-sync -f

[Unit]
Description=Airband Real-time Recording Sync
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=emilio
WorkingDirectory=/home/emilio/atc-transcriber
EnvironmentFile=/home/emilio/.config/airband-sync.env
ExecStart=/home/emilio/.cargo/bin/uv run /home/emilio/atc-transcriber/script/airband_realtime_sync.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
