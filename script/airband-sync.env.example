# Environment configuration for airband_realtime_sync.py
# Copy this to ~/.config/airband-sync.env and configure

# Directory where airband writes recordings
AIRBAND_RECORDINGS_DIR=/home/emilio/airband-recordings

# Cloudflare R2 configuration
R2_ENDPOINT=https://311c8a2fcce3b5bdc7962b9a2cdd8c97.r2.cloudflarestorage.com
R2_BUCKET=radio-recordings
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key

# Optional: prefix path in R2 bucket (leave empty for root)
# R2_PREFIX=

# Rails API base URL
API_BASE_URL=https://your-app.com

# API authentication token (same as ASR worker token)
ASR_WORKER_TOKEN=your-secret-token-here

# Optional: Path to ffprobe (will search PATH if not specified)
# FFPROBE_PATH=/usr/bin/ffprobe
